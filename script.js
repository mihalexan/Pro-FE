function toggleFullList() {
  var fullList = document.getElementById("fullList");
  if (fullList.style.display === "none") {
    fullList.style.display = "block"; // Показать элемент fullList
  } else {
    fullList.style.display = "none"; // Скрыть элемент fullList
  }
}
